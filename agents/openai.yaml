interface:
  display_name: "VSSH"
  short_description: "Operate remote infra with vssh safely"
  default_prompt: "Use $vssh to inspect or operate the configured SSH target safely through familiar remote commands."
